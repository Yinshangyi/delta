export function App() {
  return (
    <main className="mx-auto flex min-h-dvh max-w-2xl flex-col justify-center gap-3 px-6">
      <p className="text-muted font-mono text-xs tracking-widest uppercase">Delta</p>
      <h1 className="text-3xl font-semibold tracking-tight text-balance">
        When do we reach the goal?
      </h1>
      <p className="text-muted">Scaffolding only. The application shell lands in SHL-01.</p>
    </main>
  )
}
