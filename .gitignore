node_modules/
dist/
.cache/
.direnv/
result
.DS_Store
*.local
.env

# Real figures, kept out of the repository — see seed.local.md
*.local.md

# Screenshots vitest captures when a browser test fails — regenerated per run.
.vitest/

# rtk's command history — local, and none of the repository's business.
.claude/cache/
